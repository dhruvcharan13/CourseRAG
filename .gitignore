# Reference repo (cloned from the internet — not part of CourseRAG)
/mcp-local-rag/

# Runtime data root (default from config)
/course-kb/

# Source PDFs for the eval corpus — copyrighted course material, never committed.
# The eval set (evals/) records which files and pages it labels, so the baseline
# stays reproducible without shipping the documents themselves.
/corpus/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.eggs/
